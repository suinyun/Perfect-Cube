<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.team27.perfectcube">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.LoginActivity" />
        <activity android:name=".controller.RegisterActivity" />
        <activity android:name=".controller.ListActivity" />
        <activity android:name=".controller.SingleListItem"
            android:label="Single Item Selected"></activity>
        <activity android:name=".controller.SearchActivity" />
        <activity android:name=".controller.ReservationActivity" />
        <activity android:name=".controller.MapViewActivity" />

        <!--Maps Android Key-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA0ClMU396RbjOJ7753m9Ichp1bfYBCz_k"/>

        <!--Maps-->

    </application>

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"
        />

</manifest>