# Uncomment the next line to define a global platform for your project
platform :ios, '9.3'

target 'Shelter_Finder' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Shelter_Finder

  pod 'Firebase/Core'
  pod 'Firebase/Auth'

  target 'Shelter_FinderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Shelter_FinderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
